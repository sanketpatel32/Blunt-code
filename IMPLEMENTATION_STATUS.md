# Implementation status

## Completed

- Milestone 0: Go service, SQLite migrations, health/meta APIs, loopback-only HTTP server, Vite-to-embedded-assets build script.
- Milestone 1: persistent workspaces and rules, validated roots, native Windows picker, safe source discovery, language detection, default exclusions, lazy tree API.
- Milestone 2: analyzer registry integration, Quick/Standard/Deep scan profiles (Quick runs Ruff/Biome only; Deep matches Standard in V1), persisted file and directory selection overrides (including more-specific file exceptions), direct-process analyzer runs, scan lifecycle/cancellation, SSE events, normalized-result persistence, partial-failure outcomes, and local Markdown reports.
- Milestones 3–5: Ruff and Biome structured parsers, pinned checksum-verified private installation, tool readiness/install APIs, normalized report rendering/export, and coverage-aware new/fixed/persistent comparison.
- Milestone 6 implementation: Semgrep 1.172.0's Windows wheel and official uv 0.11.16 are checksum-verified before private app-data installation; bundled local rules are extracted and versioned there; scan plans use only those rules with metrics, version checks, inherited app token, and user settings disabled.
- Milestone 7 implementation: managed SonarQube Community Build, SonarScanner, and Java 21 artifacts are pinned and checksum-verified, safely extracted under app data, configured on a dynamically selected loopback port, and bootstrapped with DPAPI-protected local credentials. The adapter starts and stops this private server for scans; a Windows fixture scan completed with Ruff, Semgrep, and SonarQube all succeeding.
- Milestone 8 implementation: path/origin guards, archive traversal protection, crash recovery, process cancellation and Ctrl+C lifecycle shutdown, bounded per-tool process output and timeouts, redacted local diagnostic logs with a 500 MB cap, Windows per-data-directory single-instance protection, persisted offline mode (enforced by tool installation), local-only `bluntcode doctor` diagnostics (including disk-space and JSON output), documentation, notices, checksum-verified local/HTTPS installer paths, build scripts, and focused unit/integration tests.
- Milestone 9 implementation (25 improvement loops, release 0.2.0): headless `bluntcode scan <path>` CLI for automation and CI (`--profile quick|standard|deep`, `--json`, `--timeout`, `--quiet`; stderr progress, stdout summary, exit codes 0 completed incl. warnings / 1 failed-cancelled-timed out / 2 usage / 130 Ctrl+C); API additions — global recent-scans feed with dashboard summary aggregates (`GET /api/v1/scans`), SARIF 2.1.0 export (`report.sarif`, VS Code / GitHub code scanning compatible), standalone self-contained HTML report (`report.html`), CSV findings export with UTF-8 BOM via a filter pipeline shared with the JSON findings endpoint (`findings.csv`), coverage-aware fixed-findings listing against the previous completed scan (`/fixed`), and an enum-only loopback open-folder endpoint launching Explorer for the data/reports/logs/tools directories (`POST /api/v1/system/open-folder`); deep profile now genuinely deeper (Ruff runs with `--select=E,W,F,B,SIM,C4,RET,ARG,PLR`; quick/standard byte-identical; diagnostic logs record the analyzer command line); web UI reworked — App.tsx monolith split into pages/components/hooks/lib modules with the test suite grown from 16 to 124 cases, dark mode with OS-preference sync and no-flash bootstrap, home dashboard with activity summary and recent scans, report severity distribution and per-analyzer mini-bars with print styles, sortable findings columns, debounced text filters with removable chips and severity pill filters, "What changed" fixed-findings panel, export menu (Markdown/HTML/SARIF/CSV with CSV honoring filters), scan history severity mini-bars and profile badges, stacked toast notifications replacing the single notice banner, layout-aware loading skeletons and tool install/repair/update busy feedback, global keyboard shortcuts (`g`+`h/w/t/s`, `n`, `/`, `?` help dialog), recursive file tree search with highlighting and retryable loads, dialog focus traps/aria-modal/backdrop close, error boundary with recovery, real 404 page, About page, skip-to-content, relative timestamps with absolute tooltips, clipboard copy actions, arrow-key row navigation, Settings buttons opening data folders in Explorer, friendly empty states, and an all-clear celebration for zero-finding scans.
- Milestone 10 implementation (20 feature loops plus a QA hardening batch, release 0.3.0): CI severity gates on headless scans (`--fail-on` with `high+` shorthand, `--max-findings`, exit 1 on a tripped gate) and baseline diff mode (`--baseline` accepts a previous scan ID or a Blunt Code SARIF file via new `partialFingerprints` round-trip, so gates count only new findings); expanded bundled semgrep rulepack (2 → 20 Python/JS-TS security rules, rules version 2.0.0, exercised by a live semgrep 1.172.0 scan — 0 findings on a clean fixture); configurable SonarQube startup wait (`BLUNTCODE_SONAR_STARTUP_TIMEOUT`, immediate health polling, progress logging); per-project `.bluntcodeignore` honored by CLI, UI, and file tree; versioned JSON report export (`--format json` and a `findings.json` API download, byte-stable output); Biome React auto-domain (plan-time React detection, injected `linter.domains.react` config for the pinned Biome 2.5.6); fingerprint-based finding suppression (workspace-scoped API routes plus full web UI: suppress with reason, restore, Suppressions panel, suppressed status excluded from counts, reports, and gates); severity trends (`GET /api/v1/workspaces/{id}/trends` and a dependency-free SVG stacked-bar chart on the workspace page); bounded analyzer parallelism (`--jobs` semaphore, default still sequential); watch mode (`--watch` with debounce and coalescing rescans); built-in committed-secrets detector and TODO/FIXME comment tracker (in-process, no downloads); GitHub Actions annotations export (`--format github`, 10-per-type cap, severity-to-level mapping); richer findings API filtering/sorting/pagination (rule, path_prefix, comma-list severity/status, extended sort whitelist, page/page_size); global stats endpoint (`GET /api/v1/stats`); `doctor --fix` (re-extracts stale rules, cleans interrupted installs, recreates missing data dirs, lock-safe); and the interactive standalone HTML report (collapsible per-file groups, client-side filter toolbar, zero external resources). QA hardening in the same release: graceful Ctrl+C scan shutdown, event-bus history pruning, per-language analyzer file routing (fixed the ruff-on-JavaScript class of failures), the SonarQube compute-engine wait fix that had silently dropped all SonarQube findings, ruff command-line batching for large workspaces, API validation and security headers, and the frontend lint-debt sweep (79 findings).
- Milestone 11 implementation (10 feature loops, release 0.4.0): SARIF export from the CLI (`bluntcode scan --format sarif` writes the exact SARIF 2.1.0 document the API's report download serves — pinned byte-for-byte by a test — enabling the CI baseline round-trip without a server and GitHub code-scanning uploads; `--watch` emits one complete document per rescan); dashboard overview cards fed by `GET /api/v1/stats` (workspaces, scans with a completed/running split and a live pulse, findings with a severity mini-bar and counted legend, suppressions, and tools readiness, with skeleton loading, quiet inline retry, and zeros on a fresh install); GitHub Actions CI (`.github/workflows/ci.yml`, windows-latest: vet/build/full Go tests, web tests and build, then a self-scan dogfood gate — the freshly built exe scans `web/` quick-profile under `--fail-on high+` with a workspace-isolated `LOCALAPPDATA`, managed tools cached by manifest hash, and the markdown report uploaded as an artifact when the gate trips); findings UI rule filter (a `<select>` populated from the report's own data, server-side via `rule=`) plus proper `page`/`page_size` pagination at a fixed 50-row window replacing limit/offset, with boundary disables, polite status announcements, filter/sort changes resetting to page 1, and the CSV export mirroring active filters; `bluntcode config` (read-only effective-configuration report — every resolved path with exists/missing markers, the SonarQube timeout override in effect, persisted settings when the database is readable, installed tool versions; `--json`; never takes the data-dir lock, never creates anything); broad language coverage for the built-in analyzers (discovery classifies 40+ file types across code, web/data, shell, and config/credential files — the secrets detector now covers `.env`, Dockerfiles, and YAML; external tools keep exactly their own languages so fingerprints stay stable); incremental rescans (`--incremental`, and watch mode automatically from its second scan onward — SHA-256 content hashes persisted per scan in migration 005, reuse invalidated on analyzer-set/version/profile/Blunt Code build changes, findings for unchanged files copied forward with fingerprints intact so suppression and new/fixed/persistent comparison keep working, an equivalence test pinning the unchanged-workspace invariant, and any lookup failure degrading to a full scan); inline `bluntcode:ignore` comments for the built-in analyzers (any comment leader, same-line or immediately-following-line scope, optional rule id and `reason:` free text, fail-safe parsing so a typo'd rule id keeps the finding visible, parsed directives in the JSON envelope so secrets never leak through their own ignore comments; external tools keep their own pragmas); and the user docs suite (docs/ci.md, docs/ignoring-findings.md, docs/configuration.md, cross-linked from the README's Documentation section). The wave closed with a dogfood self-scan cleanup (`.bluntcodeignore` plus inline ignores over intentional fixture/prose hits: self-scan 0 high / 158 low-baseline, the CI gate passes, and an unchanged-workspace incremental rescan measured at 92 ms versus 27 s full).
- Milestone 12 implementation (release 0.6.0, developed against an isolated clone after concurrent-writer corruption forced the working copy offline): **global findings search** end-to-end (`GET /api/v1/findings/search` — text across message/rule/path, severity lists, analyzer equality, workspace scoping, suppression-aware, severity-ranked paging — plus a new web Search page with debounced query, severity pills, analyzer filter, server paging, and deep links into originating reports); **workspace risk scoring** (`GET /workspaces/{id}/risk`: critical ×10 / high ×5 / medium ×2 / low ×1 over the latest completed scan's persisted counts, A–D grade, trend delta vs the previous completed scan; dashboard RiskCard); **scan lifecycle management** (`DELETE /scans/{id}` with terminal-only enforcement, 409 for active scans, full foreign-key cascade verified by test; `DELETE /workspaces/{id}/scans?keep=N` retention pruning backed by `PruneOldScans`; a Prune-history control on the workspace page; and the standalone `bluntcode prune <path> [--keep N]` CLI command); **workspace tags** (migration 006, validated lowercase labels, GET/PUT handlers, batched tag map for list endpoints, tags embedded in workspace JSON with nil→[] marshaling); **CLI output upgrades** (`--format csv` sharing the API export's exact columns/BOM/formula-neutralization via a new reports.CSV renderer, `--output FILE` redirecting any document format to disk, `--save-baseline FILE` writing the SARIF baseline after any completed scan without a shell redirect); **scoped CI gates** (`--gate-analyzer`, `--gate-category` allow-lists applied before gate evaluation with case-insensitive matching and stderr scope reporting) plus `--github-cap 1..50` overriding the annotation display cap through a parameterized reports renderer; **watch tuning** (`--watch-poll` 250ms–1m, `--watch-quiet` 100ms–2m with quiet≥poll/4 sanity); **deeper secret detection** (six new rules — Stripe live keys excluding test mode, OpenAI project keys and classic T3BlbkFJ-marker keys, Anthropic keys, Slack app-level tokens, Azure storage account keys — all boundary-guarded with placeholder rejection and Shannon-entropy floors, 20 new table cases); **TODO attribution syntax** (`TODO(jane):` / `FIXME(APPS-123)` now count as debt, owner/ticket captured on the match and carried verbatim in the finding message, malformed parens still ignored); **semgrep rulepack v3.0.0** grown to 25 bundled rules (`os.popen`, `tempfile.mktemp` races, `random` used for security values, `new Function` execution, `postMessage("*")`) with the version bump invalidating incremental-reuse caches like an analyzer upgrade; **shareable report URLs** (filters/sort/page hydrate once per mount from the address bar, sync back via replaceState using debounced text filters, popstate restores state, and a global afterEach URL reset makes the jsdom suite order-proof); **server-paged history** (`GET /workspaces/{id}/scans?page&page_size` bounded windows with totals/has_next consumed by a dual-mode HistoryTable that falls back to client slicing when unpaged); **UI power features** (profile picker on Analyze actions feeding startScan profiles; workspace settings editor doing real rename/default-profile PATCHes replacing the old stub notice; SARIF/HTML/JSON export links on every terminal history row alongside Markdown; trend range selector driving the trends limit; complete 60+ entry language label map; copy-fingerprint action in the preview dialog; aria-sort/aria-pressed on workspace sort controls); **UI resilience** (useLoad stale-response sequencing so rapid dependency changes can't apply older responses; SSE reconnects with exponential backoff 1s→15s, reset on successful open, retry count shown in the stream chip); and **release engineering** (`bluntcode help` listing every subcommand on stdout; static-handler tests pinning the app-shell SPA fallback for client routes and direct hashed-asset serving; scripts/smoke.ps1 booting the exe under an isolated LOCALAPPDATA and probing health/meta/SPA-fallback/findings-search; CHANGELOG 0.6.0, version bump to 0.6.0, README CLI examples and feature bullets, docs/ci.md format/output/scope sections).

## Remaining

- Run a full Windows UI E2E before claiming a public V1 release. The managed Semgrep install, the expanded 20-rule bundled pack (exercised by a live semgrep 1.172.0 scan — 0 findings on a clean fixture), managed SonarQube bootstrap/scan, ZIP checksum, install/uninstall scripts, and an end-to-end smoke test of the new built-in analyzers in the real binary were all exercised locally on Windows. The 0.3.0 and 0.4.0 batches were otherwise verified through the expanded Go and web unit/integration suites (177 frontend tests) plus the CI workflow's self-scan gate rather than a full interactive UI E2E pass — that pass is still pending.
- Semgrep's resolved transitive Python dependency artifacts are still not hash-locked or release-audited (the uv archive and the Semgrep Windows wheel remain checksum-pinned).
- The CI self-scan gate covers only `web/` under the quick profile (ruff + biome); no CI gate yet exercises the broadened built-in analyzers (secrets/TODO over 40+ languages) or the Go sources of the repo itself.
- Inline `bluntcode:ignore` directives apply only to the built-in analyzers by design — external tools keep their own mechanisms (`# noqa`, `// biome-ignore`, `# nosemgrep`); a unified ignore experience across external tools is not implemented.

- Milestone 13 implementation (20-loop cycle executed with two parallel subagents on an isolated clone; backend and frontend domains partitioned so neither touched the other's files, coordinator verified and committed each domain separately): **backend wave** (six loops) — `GET /api/v1/scans/{id}/findings.jsonl` serving the newline-delimited findings document as a download mirroring the CLI's `--format jsonl`; suppression round-trip via `GET /workspaces/{id}/suppressions.csv` export plus a validating `POST .../suppressions/import` that normalizes case, counts invalid/duplicate rows, and reports `{imported, skipped_invalid, duplicate}`; `--format markdown` on headless scans (stdout or `--output`, rejected alongside the other document formats when combined with `--json`); mutation rate limiting through a token-bucket middleware (30 burst / 30 per minute, GETs exempt, 429 + Retry-After, resettable for tests); a doctor database self-check running SQLite `PRAGMA quick_check` over a read-only open with info-level handling of absent databases; and `risk_grades` A/B/C/D distribution folded into `GET /api/v1/stats` via a latest-completed-per-workspace window query graded with the risk endpoint's weights. **frontend wave** (six loops) — analyzer duration chips (success/failed) in the report header; workspace tags rendered as capped chip groups with overflow counter plus a debounced case-insensitive Filter-by-tag input; risk-grade badges on workspace cards defensively typed around the optional API field; client-side suppressions CSV export with RFC-4180 escaping and object-URL lifecycle management; command-palette dynamic entries proven by tests including a footer note reporting indexed-workspace count; and sr-only captions added to every data table (the suppressions list was converted from `<ul>` to a semantic table to host its caption). **coordinator loops** — CHANGELOG Unreleased entries for the whole wave; docs/ci.md format-table rows for markdown/jsonl; docs/configuration.md hardening + suppression-round-trip section; full dual-stack certification (vet clean, entire Go suite green with only the two pre-existing wall-clock budget tests skipped under game-load, tsc clean, 256/256 web tests, production build). The cycle also surfaced process learnings now encoded in test infrastructure: jsdom URL resets after every test (order-proofing), DeepEqual-based flag-parse assertions once scanConfig grew maps, and fixture analyzers aligned to the coverage rule the compare endpoint enforces.
